var dataInteresse;
var dataReferencia;
const NR_ARGUMENTOS;//Numero de argumentos
const NR_DIAS_SEMANA;

function receberData(data){
    console.log("insira a data");
    data = 20181107
    function verificarData(data);
    return data;
}

function dataInteresse(){
    console.log("data de interesse");
    dataInteresse = receberData();
    return dataInteresse;
}

function verificarAnoBissexto(){

}

function dataReferencia(){
    console.log("Informe a data de referencia");
    data = receberData();
    return dataReferencia;
}

function semana(data){
    var semana;
    var semanaData;
    return semana;
}

function execucao(){
    dataInteresse();
    verificarAnoBissexto();
    dataReferencia;
    semana();
}

function verficarArgumentos(argumentos,NR_ARGUMENTOS){
    for(i = 0; i< NR_ARGUMENTOS; i++) {
         verificarNumeroInteiro(argumentos);
         verificarNrNegativo(argumentos);
         if(i> NR_ARGUMENTOS/2){
             verificarNumeroInteiroSemana();
             verificar
         }
    
function verificarNumeroInteiro(data){
    if(data%1000 <0 || data> 9999)
        return -1;
    if(data%100 < 0 || data> 12)
        return -1;   
    if(data%1000000 || data === 0)
        return -1;
    if(data%10 ===0 || data > 31 )
        return -1;
    if(verificarAnoBisexto(data) == true)
        return -1
    }

function computarData(dataReferencia){
    semana = semanaReferencia(dataReferencia);
    return semana;
}