//ilustração de documentação

let idade = 10; 

let maioridade = idade >= 18;

/**
 * a recomendação JSDoc considera somente apos dois paramentros apos /
 * @constructor
 * @param {string} title - Introduction the Software Build.
 * @param {string} author - Antonio Tavares.
 * @param {string} teacher - Fábio Nogueira.
 */
function log(title, author,teacher) {
    console.log(title, author,teacher)
}
